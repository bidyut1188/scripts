echo "Package: $(cat package.idx | shuf | head -n 1)"
echo "Version: $(cat version.idx | shuf | head -n 1)"
echo "Maintainer: $(cat maintainer.idx | shuf | head -n 1)"
echo "Installed-Size: $(cat insize.idx | shuf | head -n 1)"
echo "Depends: $(cat depends.idx | shuf | head -n 1)"
echo "Conflicts: $(cat conflicts.idx | shuf | head -n 1)"
echo "Breaks: $(cat breaks.idx | shuf | head -n 1)"
echo "Replaces: $(cat replaces.idx | shuf | head -n 1)"
echo "Homepage: $(cat homepage.idx | shuf | head -n 1)"
echo "Download-Size: $(cat dosize.idx | shuf | head -n 1)"
echo "APT-Sources: $(cat aptsources.idx | shuf | head -n 1)"
echo "Description: $(cat description.idx | shuf | head -n 1)"
